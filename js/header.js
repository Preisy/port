window.onload = tent();function tent() {    var headerHeight = document.querySelector('.header')    headerHeight = Math.max(        (headerHeight.scrollHeight || 0),        (headerHeight.offsetHeight || 0),        (headerHeight.clientHeight || 0)    )    var tent = document.getElementById('tent')    tent.style.cssText = "height: " + headerHeight + "px;"    //preloader    if (!!document.getElementById('preloader'))        setTimeout(function () {            document.getElementById('preloader').style.cssText = "opacity: 0;"            var timer = setTimeout(function () {                document.getElementById('preloader').remove();            }, 400);        }, 100);}